% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xyt-package.R
\docType{package}
\name{xyt-package}
\alias{xyt}
\alias{xyt-package}
\title{Extract raster time series values at points in space and time}
\description{
The package has one job: given a series of rasters that is described by a
\emph{reader function} rather than held in memory, and a set of points that each
carry their own date-time, return the value of the series at each point.
}
\details{
The series can be far larger than memory, and can live anywhere the reader
can reach, because only the time slices actually needed are read, and each
is read exactly once.
}
\section{The reader contract}{


A reader is any function supporting two calls.

\preformatted{
  read(returnfiles = TRUE, ...)        # -> data.frame with a `date` column
  read(date, inputfiles = files, ...)  # -> single-layer SpatRaster
}

The first call is the catalogue: one row per available time slice, in
ascending date order. The second reads one slice. \code{inputfiles} is the
catalogue handed back so the reader does not have to rebuild it.

Use \code{\link[=xyt_reader_check]{xyt_reader_check()}} to test a function against the contract before
handing it to \code{\link[=extract_xyt]{extract_xyt()}}.
}

\seealso{
\code{\link[=extract_xyt]{extract_xyt()}}, \code{\link[=xyt_reader_check]{xyt_reader_check()}}, \code{\link[=synthetic_reader]{synthetic_reader()}}
}
\author{
\strong{Maintainer}: Michael D. Sumner \email{mdsumner@gmail.com} (\href{https://orcid.org/0000-0002-2471-7511}{ORCID})

Authors:
\itemize{
  \item Michael D. Sumner \email{mdsumner@gmail.com} (\href{https://orcid.org/0000-0002-2471-7511}{ORCID})
}

}
\keyword{internal}
