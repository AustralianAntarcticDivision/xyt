% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-xyt.R
\name{extract_xyt}
\alias{extract_xyt}
\title{Extract values from a raster time series at points in space and time}
\usage{
extract_xyt(
  read,
  xyt,
  ctstime = FALSE,
  when = c("nearest", "previous"),
  method = c("simple", "bilinear"),
  fact = NULL,
  crs = "EPSG:4326",
  files = NULL,
  tolerance = NULL,
  verbose = interactive(),
  ...
)
}
\arguments{
\item{read}{a reader function. See the reader contract in \link{xyt} and
\code{\link[=xyt_reader_check]{xyt_reader_check()}}.}

\item{xyt}{a data.frame or matrix with three columns: x, y and time. Extra
columns are ignored. The time column may be POSIXct, Date or character.}

\item{ctstime}{interpolate linearly in time between bracketing slices
(\code{TRUE}), or take the value from one slice (\code{FALSE}, the default).}

\item{when}{which slice a point takes its value from when
\code{ctstime = FALSE}: the closest in time (\code{"nearest"}, the default) or the
most recent one at or before it (\code{"previous"}).}

\item{method}{how the value is taken from the raster: \code{"simple"} for the
value of the cell the point falls in, \code{"bilinear"} for a distance
weighted average of the four nearest cell centres.}

\item{fact}{optional aggregation factor applied to each slice before
extraction, as in \code{\link[terra:aggregate]{terra::aggregate()}}. One integer, or two for the
horizontal and vertical factors.}

\item{crs}{coordinate reference system of the coordinates in \code{xyt},
defaulting to longitude/latitude on WGS84. Points are transformed to the
raster's own system before extraction.}

\item{files}{the catalogue, if you already have it: a data.frame with a
\code{date} column. Supplied here, \code{read(returnfiles = TRUE)} is never called,
so a reader only has to do the one job of reading a slice. See also
\code{\link[=xyt_source]{xyt_source()}}.}

\item{tolerance}{how far, in days, a point may sit from the slice matched
to it before its value is refused and returned as \code{NA}. \code{NULL}, the
default, derives it from the spacing of the series.}

\item{verbose}{report progress as slices are read.}

\item{...}{passed to \code{read}, and only to \code{read}.}
}
\value{
a numeric vector with one value per row of \code{xyt}.
}
\description{
Each row of \code{xyt} is a location and a date-time. The value returned for it
is read from the time slice nearest that date-time, or, with
\code{ctstime = TRUE}, interpolated linearly between the two slices that bracket
it.
}
\details{
Only the slices that some point actually needs are read, and each of those
is read exactly once, however many points fall on it and whatever order the
rows arrive in.

\code{...} reaches the reader alone. This is a deliberate departure from the
version of this code that lived in raadtools, where \code{...} was passed to
both the reader and the extraction, so that an argument meant for one was
silently offered to the other. Arguments controlling the extraction are
named here instead.
}
\examples{
read <- synthetic_reader()
xyt <- data.frame(x = c(0.5, 2.5), y = c(0.5, 3.5),
                  t = as.POSIXct(c("2000-01-01", "2000-01-05"), tz = "UTC"))
extract_xyt(read, xyt)
extract_xyt(read, xyt, ctstime = TRUE)
}
