% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic.R
\name{synthetic_reader}
\alias{synthetic_reader}
\title{A reader function with no data behind it}
\usage{
synthetic_reader(
  n = 10L,
  start = "2000-01-01",
  by = "1 day",
  crs = "EPSG:4326",
  extent = c(0, 4, 0, 4),
  res = 1
)
}
\arguments{
\item{n}{number of time slices.}

\item{start}{date-time of the first slice.}

\item{by}{spacing between slices, as understood by \code{\link[=seq.POSIXt]{seq.POSIXt()}}.}

\item{crs}{coordinate reference system of the slices.}

\item{extent}{extent of the slices, as xmin, xmax, ymin, ymax.}

\item{res}{cell size.}
}
\value{
a function of the form \code{read(date, returnfiles = FALSE, inputfiles = NULL, offset = 0, ...)}. \code{offset} is added to every value,
and is there so a test can prove that \code{...} reaches the reader.
}
\description{
Builds a reader that satisfies the contract \code{\link[=extract_xyt]{extract_xyt()}} expects, over
a small in-memory series whose values are known in closed form. It exists
so the extraction machinery can be exercised, and its answers checked
against arithmetic, without any files, network or credentials.
}
\details{
Each slice holds the value

\preformatted{   value = 100 * x + y + i }

where \code{x} and \code{y} are the coordinates of the cell centre and \code{i} is the
one-based position of the slice in the series. Two properties follow, and
both are used in the tests. The field is linear in \code{x} and \code{y}, so
\code{method = "bilinear"} returns \code{100 * x + y + i} exactly anywhere in the
interior. And it is linear in \code{i}, so \code{ctstime = TRUE} at a fraction \code{p}
between slices \code{i} and \code{i + 1} returns exactly \code{100 * x + y + i + p}.
}
\examples{
read <- synthetic_reader()
head(read(returnfiles = TRUE))
read(as.POSIXct("2000-01-03", tz = "UTC"))
}
