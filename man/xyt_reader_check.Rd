% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reader-contract.R
\name{xyt_reader_check}
\alias{xyt_reader_check}
\title{Check a function against the reader contract}
\usage{
xyt_reader_check(read, error = TRUE, ...)
}
\arguments{
\item{read}{the function to check.}

\item{error}{stop on failure (the default), or return the report quietly.}

\item{...}{passed to \code{read}, exactly as \code{\link[=extract_xyt]{extract_xyt()}} would pass it.}
}
\value{
invisibly, a data.frame with one row per check: \code{check}, \code{ok} and
\code{note}.
}
\description{
Runs the calls \code{\link[=extract_xyt]{extract_xyt()}} will make, in the order it will make them,
and reports what came back. Use it when wiring up a new source, so a
mismatch is a sentence about the contract rather than an error from inside
the extraction loop.
}
\examples{
xyt_reader_check(synthetic_reader())
}
